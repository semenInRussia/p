#lang info

(define collection "p")
(define pkg-authors '(semenInRussia))
(define version "0.0")

(define build-deps '("racket-doc"
                     "rackunit-lib"))

(define pkg-desc
  "Small shell utility fow printing files and printers management.")

(define license '(Apache-2.0))

